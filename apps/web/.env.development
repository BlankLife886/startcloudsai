# 本地开发：/api 由 Vite 代理到本地后端。
# 当前后端通过 Docker Compose 运行，唯一对外入口是网关 127.0.0.1:8081（见根目录 .env 的 GATEWAY_PORT）。
# 如果改为在宿主机直接 go run ./cmd/server serve（监听 :8000），把下面这行改回 http://localhost:8000。
VITE_API_PROXY_TARGET=http://localhost:8000
