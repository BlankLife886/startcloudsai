# React 主站预览

这个应用只用于逐页迁移和视觉回归，不承载当前生产路由。

```bash
npm install
npm run dev
```

开发入口为 `http://127.0.0.1:3105/__migration`。共享静态资源来自 `apps/web/public`，页面实现通过基线后才允许加入路由表。当前已迁移公开页面、拼图、本地图片压缩、问题反馈、每日签到、创作历史、钱包、账户设置和通知中心，并建立了 React 登录路由守卫；生产 Vue 路由尚未切换。
